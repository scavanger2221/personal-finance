-- Create database if not exists (handled by postgres container env vars, but can add custom SQL here)
-- Additional initialization SQL can be added below

-- Example: Create additional users or set up extensions
-- CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
